# Google AI Studio / Gemini API key (server-only; never expose to the client)
GEMINI_API_KEY=

# Optional: defaults to gemini-2.5-flash (stable)
# GEMINI_MODEL=gemini-2.5-flash

# Neon Postgres connection string (server-only)
DATABASE_URL=
